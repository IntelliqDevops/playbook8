---
- name: Create user on all the servers
  hosts: all
  tasks:
    - name: User creation
      user:
        name: Ravi
        password: intelliqit
        uid: 8989
        home: /tmp/Ravi
        comment: "a normal user"
        shell: /bin/bash

...
