---
- name: Install git and download a remote git repository
  hosts: all
  tasks:
    - name: Install git
      apt:
        name: git
        state: present
        update_cache: yes
    - name: Download a remote git repo
      git:
        repo: https://github.com/IntelliqDevops/maven.git
        dest: /tmp/mygit
...
